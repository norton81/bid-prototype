import { SharedCellContextDirective } from './shared-cell-context.directive';

describe('SharedCellContextDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedCellContextDirective();
    expect(directive).toBeTruthy();
  });
});
